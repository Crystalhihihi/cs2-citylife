# 交接文档 · 2026-08-20 深夜版

> 写给明天（或任何接手者）：当前状态、怎么验证、已知坑、下一步。设计权威见 `docs/specs/2026-08-16-cs2-living-city-design.md`（v4，含 §12 决策记录与 §13 角色层）。

## 一、当前形态（全部已部署到本地 Mods/CityLife，git 已入库）

**信息层**：自建信息流面板（GameTopLeft 气泡按钮）——LLM 批量帖（人格卡×形态×真实业态锚点）+ 评论区（神评/抬杠/共情分工）+ 板块 tab（综合/突发/热帖/连载）+ 市长发帖框 + 回复 + 拖拽缩放 + 自绘滚动条。原版 Chirper 被发布侧 Harmony 补丁根治（实体级删除）。

**写回层（M4 活动链，本次重点验收对象）**：
市长发帖说办活动 → 意图解析（LLM 归一化 startHour/durationH/scale/budget）→ **原生弹窗确认（含场馆选择器、时段警示）** → 真扣财政（时长平方级加价）→ 公告 → 下一个整点开场 → 吸引力 ramp + 分批注入 → 实数到场 → 结算（爆棚返 50%/及格 20%/冷场骂）→ 参数恢复。阴间时段（0-6 点）人气系数 0.15 且必挨骂。

## 二、明天验证清单（按序）

1. 重启游戏，日志（`Logs/CityLife.log`）无 CRITICAL、有 `[Filter] 原版 chirp 过滤补丁已应用`
2. 市长发帖："明晚 8 点在中央公园办个周末市集，预算中档" → 弹窗应出现，**场馆选择器可切换**（修的就是"活动被随机扔到看不见的地方"）
3. 确认后日志看 `[Money] 财政 -XXX`（真扣）→ `[Event] 已排期 明晚 20:00`
4. 到点看现场：`[Event] 开场` + 官方帖可点"前往现场"；场馆应有人群聚集（19-21 点黄金档系数 1.0）
5. 结算帖：爆棚/及格/冷场 + 下一炉市民议论活动（【活动结果】上下文）
6. **对照组**（可选但强烈推荐）：凌晨 3 点办一场——确认卡应出红色"时段阴间"警示，到场应惨淡，结算必挨骂
7. 面板交互回归：拖拽/缩放/滚动条/贴底（上翻时来新帖只亮气泡）/中文输入不豆腐块

## 三、已知待校准项（玩的时候留意日志）

- `[Anchor·校准]`：公司盈利分布（阈值 200/50 是猜的）与业态词命中率——攒半小时日志可校准
- 吸引力 boost +100 是初估值（`EventChainSystem.BeginRunning`），看人群到场速度是否偏弱/过强
- 晚高峰 API 偶发 >240s 尖峰（已调 420s；再超时有日志 `[LLM] 一炉失败`），面板短暂安静属供给侧现象

## 四、配置文件（游戏用户目录 ModsSettings/CityLife/）

- `llm.json`：供给（当前 DeepSeek 官方 v4-flash + thinking=disabled；备胎 Kimi Code CLI 轨/K2.7）
- `settings.json`：`t0Fallback`（默认 false）/ `feedMode`（always|openOnly|throttled）/ `writeBackTier`（mild|normal|crazy）/ `feedMaxItems`（默认 100）
- `personas.jsonl` / `usernames.jsonl` / `eventpacks.jsonl`：风格卡/网名/事件包的玩家覆写口（schema 见对应源码头注释）

## 五、下一步 backlog（按 v4 排序）

1. EventJournal 分类学摸底（等城里出事，`[News]` 日志喂真名后扩事件映射表）
2. M5 明星市民（人格记忆+连续剧；Crystalhihihi 转永久 L2）
3. 民意层 v2（请愿→聚集市政厅，活动链状态机的第二个事件包）
4. M3 气泡（街头对话 overlay，最高技术风险）
5. 城市日报/编年史、拆迁悼词、隔壁市长（沉淀层，见 v4 §13 与拷问会记录）

## 六、铁律速查（新接手必读 AGENTS.md 全文）

SystemAPI 禁用（构建不跑源码生成器）｜更新间隔必须 2 的幂｜游戏系统开关别乱关（CreateChirpSystem 断言教训）｜版本敏感面只进 GameBridge｜LLM 不直接改数值（单向阀门+分档上限）｜绝不阻塞模拟线程｜prompt 固定前缀+动态尾部
