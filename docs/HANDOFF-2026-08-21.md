# 交接文档 · 2026-08-21 深夜版

> 写给下次接手（可能几天后）。设计权威见 `docs/specs/2026-08-16-cs2-living-city-design.md`（§12 决策记录到 #41）。
> 上一版交接（2026-08-20）里的功能项已全部落地验证，本版只管**新状态**与**未决现场**。

## 一、未决现场（下次开工第一件事，优先级最高）

**M3 气泡层 v1.2（commit `3390e28`）刚部署、尚未实机验证。** 世界空间渲染路线已贯通（OverlayRenderSystem，截图实锤出字"吃了吗"），v1 是从 spike 升级的正式版前身。

**验证步骤**：重启游戏 → 进城 → 街景放大 → `Ctrl+9` 开关 / `Ctrl+8` 数量档（30/60/120）：
- **看到气泡**（人白/车淡蓝/楼淡黄，跟随稳，各自换话）→ 路线定案，进正式版（见 §五）；
- **看不到** → 读日志（`Logs/CityLife.log`）`[BubbleW]` 行：`LOD 拦截：落点距离 Xm（zoom=Y）`= 收太紧就调宽；`首帧已画` 但不可见 = 写入姿势还要查；
- **文字大小**目前未控制（TMP 小字号窗口期方案因"可能把字搞没"嫌疑已撤——见 §三教训）。出字后若太大，尺寸控制是正式版第一件事。

**旧 cohtml 贴纸 spike（BubbleSpikeSystem，Ctrl+0/1/2/3）已冻结**，留作 A/B 对照，不再演进；正式版确认世界空间后拆除。

## 二、当前形态（全部已部署到 Mods/CityLife，git 入库）

- **信息层**：信息流面板（LLM 批量帖：人格卡×形态×真实店名锚点×市民真实处境×分区话题）+ 评论区（争论串@互怼+续热炉）+ 连续剧剧组（M5 v1：4 个具名角色分集推进）+ 板块 tab + 市长发帖/回复。作者署名一律真实市民名（CitizenPoolSystem 语境池，"创造条件不做限制"）。
- **感知层**：CityChangeSystem——新公司/公服/公园/地标落成、建筑被拆、新路、人口门槛 → 变化锚点进 prompt + 大事件模板快讯。信息流=市长决策的回声壁。
- **突发层**：直采事件组件（火灾=OnFire、车祸/犯罪=AccidentSite.m_Flags）→ 目击模板帖+LLM"城市快讯"帖+热议炉（两档频率闸：快讯按类限流，热议默认 12h 一次）。
- **写回层（M4 活动链）**：市长发帖办活动 → 意图解析 → 确认卡（全量公园真名候选 + **地图选点任意建筑**）→ 真扣财政 → 整点开场 → 聚集 → 结算回补。实机跑通（爆棚 315/300）。
- **民意层 v2**：负面情绪超阈值+抽签 → 请愿热帖 → 窗口 6h 内市长发帖=回应；超时聚集市政厅/地标（SignatureBuildingData 查询）→ 3h 散去结算。与活动链互斥。
- **商家广告层 v1（降级纯舆情+氛围）**：锚点信号 → LLM 广告帖（真实店名+产出业态）→ 评论互动炉 → 打折带氛围排队（Leisure 通道，老年+300/近距+200/远距-150）。**不承诺经济效果**——四轮购物 spike 实锤"制造真实购买"此路不通（§12 #40）。
- **M3 气泡层 v1**：世界空间气泡（见 §一/§三）。

## 三、世界空间渲染·全部实锤与教训（这轮彻查的结晶，勿重探）

**死因链**：`HDRP/Unlit` 不在发布 build 着色器清单 → `Shader.Find` 拿到无效 fallback → GameObject+MeshRenderer、beginCameraRendering+Graphics.DrawMesh、CustomPass+cmd.DrawMesh **三条路全是死材质陪葬**，与接入方式无关。

**正路（社区 20+ mod 验证）**：`Game.Rendering.OverlayRenderSystem`
- 写入：`GetBuffer(out deps)` → `DrawCircle/DrawCustomMesh/DrawText` → `AddBufferWriter(deps)`；
- **注册相位必须 GameSimulation**（Rendering 相位写入被其清/拷时序永久跳过——实例计数全 0 实锤）；
- **`hideOverlay` 闸**：`RenderingSystem.hideOverlay`（公开 get;set;）为 True 时连收都不收——气泡开着期间每帧压 false（渲染层开关，不碰模拟数值）；
- **世界文字**：空实体 + `NameSystem.SetCustomName` → `Buffer.DrawText(entity, pos, cameraFace:true)`（画实体渲染名，写什么画什么；CJK 零风险——游戏自带字体图集）；
- **锚点 y 用实体真实 Transform**（落点打 y=0 平面曾把气泡埋在地下；地形高度不可信）；
- **LOD 用镜头到落点距离**（裸高度不随俯仰角代表远近——实机 542m+ 全灭教训）；
- 相机权威源 = `CameraUpdateSystem.activeCamera`（Camera.main 在部分相位为 null/可能是代理）；
- **尺寸未解**：DrawText 无尺寸参；TMP 懒烘焙字号窗口期方案已撤（嫌疑）；备选=通知图标管线贴图气泡（`NotificationIconRenderSystem`：DrawMeshInstancedIndirect+Texture2DArray+`Shader Graphs/NotificationIcon`，TLE mod 复刻先例）；
- 参考实锤：`Quboid/CS2-MoveIt`（Overlays/OverlaySystem.cs 用法原文）、`krzychu124/Traffic`、`kurupted/CS2TransitView`（最简模板）、`slyh/Cities2-TrafficLightsEnhancement`（图标管线复刻）、`bworthy89/roadmod`（游戏系统反编译参考）。
- **RimWorld 对照结论**：RimTalk 气泡也是贴纸（GenMapUI 投影+IMGUI），赢在 RimWorld 把 UI 架在 OnGUI 上送钩子；CS2 无等价钩子，但 CS2 有真 3D 世界 UI（通知图标遮挡变浅实锤）——两码事。

## 四、配置与供给（游戏用户目录 ModsSettings/CityLife/）

- `llm.json`：供给（DeepSeek 官方 v4-flash + thinking=disabled；备胎 Kimi Code CLI 轨/K2.7 未实测）；
- `settings.json`：`t0Fallback` / `feedMode` / `writeBackTier` / `feedMaxItems` / `breakingNews` / `breakingHotHours` / `petitionEnabled|CooldownH|WindowH|Scale|Debug` / `shopAds` / `shopAdCooldownH`；
- `personas.jsonl` / `usernames.jsonl` / `eventpacks.jsonl`：玩家覆写口（schema 见源码头注释）。

## 五、下一步 backlog（按序）

1. **M3 正式版**（v1 验证后）：文字尺寸控制、内容管道接入（气泡文本=信息层降级产物+共位成组小剧场+场合话题+环境声锚点）、LOD 阈值标定、建造/拍照自动隐藏、设置页开关与参数；
2. **游戏内设置页**（玩家要求）：ModsSettings JSON 全部迁移到游戏原生 ModSetting 页（API 面待 dump 复核）；
3. M5 正式版明星市民（L0-L2 四级成名/星标捏人/摘要链记忆；Crystalhihihi 转永久 L2）；
4. 沉淀层：城市日报/编年史、拆迁悼词（感知器已给素材）、隔壁市长；
5. 旅游确认（游客=0 待复核）。

## 六、铁律速查（详见 AGENTS.md）

SystemAPI 禁用（构建不跑源码生成器）｜更新间隔必须 2 的幂｜游戏系统别乱关（CreateChirpSystem 断言教训）｜版本敏感面只进 GameBridge｜LLM 不直接改数值（单向阀门+分档上限）｜绝不阻塞模拟线程｜prompt 固定前缀+动态尾部吃缓存｜**永不静默失败**（拿不到相机/资源时静默 return 曾让"全灭"排查半天——诊断日志必须打）｜**一版只改一类变量，每版必有日志判决**（M3 排障轮回的教训）
