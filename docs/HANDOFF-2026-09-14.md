# 交接文档 · 2026-09-14（增量决策落档 + 场所名小修①）

**增量决策 #62-#65 已落 §12**（commit `1af70cc`）：场所名分级 / 气泡对话场景卡 / 城市记忆层 / 玩家动作反应层立项。
**开工顺序①场所名小修完成**（commit `4f3a62a`，dll 已部署 19:12），构建 0 error、PromptEval 复测无回归。②普查 spike ③对话卡 ④玩家动作反应层未动，照旧按序。

## 一、未决现场（下次开工第一件事）

**①小修待实机复验**——进存档 `tail -f Logs/CityLife.log`：
- **功能区标签不再被当店名念**：气泡/小剧场/环境摘要里不应再出现"北美低密度商业"式 zone 通用名（执行层已斩断来源；普查复查零误伤零漏网）
- 处境卡新面貌：室内/目的地带真名「」括注——**店名现在是品牌真名**（"在「Cheap & Random」里逛街""（去「Limzabar」）"，与游戏 UI 公司名同源）；无真名落类别词照旧
- 信息流锚点/广告帖店名同步变品牌真名（"Cheap & Random（神太街）"式，此前全退地址制）
- 活动场馆地图选点：选中分区自长建筑时确认卡/公告应显示"方位·115冬青街"门牌制（此前会显示功能区标签）
- 旧账复验照旧（HANDOFF-2026-09-11 §一）：猫密度/同屏上限 4 手动拉设置/传闻时效衰减毛边未动

## 二、①小修落地明细（§12 #62 第一步）

| 点 | 改动 | 文件 |
|---|---|---|
| 判定 | `IsZoneGrown` 一处定义：prefab 有 SpawnableBuildingData 且无 SignatureBuildingData（与游戏 NameSystem.GetName 同源，address-spike §3/§4 实锤；Signature 实体/prefab 两查保险） | EnvironmentDigestSystem.cs |
| 斩断 | `ShopNameOf` 分区自长建筑兜底名→null（其渲染名=zone 通用名"北美低密度商业"式功能区标签）——环境摘要聚类点/剧场 {place} 两个污染源同愈 | 同上 |
| 真名层 | `DescribeSituation`/`DestinationPlace` 加 NameSystem 参：真名「」括注>类别词；**住宅不落名**（自己家，#62 住宅无业态）；车卡（DescribeVehicle）同步传真名层 | CitizenPoolSystem.cs、BubbleChatterSystem.cs |
| 场馆 | `VenueLabel` zone-grown 改走门牌地址（"115冬青街"，与玩家选中 UI 同源），顺手补 IsUglyName 脏键闸（原代码只判空会漏 Assets. 原始键） | EventChainSystem.cs |
| prompt | 闲聊炉头新增【名字】：「」内才是真实场所名可念，无括号场所词只是业态类别禁止当店名念（执行层斩断是主闸，此行防残余双保险） | PromptBuilder.cs |
| 收编 | `EntityAnchorSystem.TryGetAddressLabel` 改为 EnvironmentDigestSystem 共享静态的实例包装（别复制粘贴） | EntityAnchorSystem.cs |

**PromptEval 复测**（chatter K=2，报告 logs/prompt-eval/eval-20260914-191238.md）：猫密度 3%/4%（前基线 5%）、Any=0、句长均值 16.9/16.5、空卡 0、近重复 0、解析丢 0——指标全保持。注：卡具是固定文本无「」真名，规则的正面收益只能实机看，离线只证无回归。

## 三、下一步 backlog（按 2026-09-12 纪要开工顺序）

1. ~~①场所名小修~~（本次完成，待实机复验）
2. **②普查 spike**（基本收口）：反编译侦察+实机 dump 工具+玩家首轮实机+分析回填全部完成（`50156e7`/`ac49413`，报告 docs/spikes/2026-09-14-census-and-event-watch.md §4 已回填）。**普查头号实锤：公司渲染名 34/34 全是 Assets. 脏键，真店名在 CompanyData.m_Brand 品牌实体**（"Cheap & Random"式，与游戏 UI 同源）——品牌真名层已落地（`f11663d`：CompanyNameOf 共享口，ShopNameOf/CompanyLabel 收编，信息流锚点/广告帖店名同愈）；住户姓实锤真姓可用（"汉密尔顿一家"式）；可入刊事件 prefab 为空（#64 灾难 watcher 用 m_Event 回指针计数，别指望 journal）；#62 斩断零误伤零漏网。**仅剩 ⑤⑥ 待正样本**：玩家开 Ctrl+7 探针后**手动放栋建筑/种棵树/拆个房子**（首轮开窗 3m43s 无操作零命中），验证自长建筑 Created 与庭院树 Owner 判别。业态细分（治"超市永远排队/住宅永远装修暖气"）字段路径已全部实锤（产出资源 34/34 可读，含开采类），可直接开做
3. ③对话场景卡（#63：成对采样、6:4 配额、A:/B: 两行各 ≤12 字、占 1 个同屏名额、车辆保持独白）
4. ④玩家动作反应层（#65：建造/拆除/美化事件→圈周边市民→快轨反应卡，复用突发快讯/传闻榜管线；监听口选型等 ②实机收口）
5. 远期：离线活人感实验矩阵（PromptEval 升级：单/多 agent×有无剧本×上下文加料×周围识别方式，盲评）；人机味专项（语气词/断句/不完整句/情绪标点，离线 A/B）

## 四、已知毛边（照旧节新增）

- 剧场 {place} 兜底=KindLabel 类型词（"商店内""住宅区窗口"），zone 标签斩断后无真名场景会填类别词，台词略生硬（"你们商店内还招人吗"）——可接受，若实机扎眼再配场景别名词
- 传闻榜时效衰减、HasInsideRoom 标定等旧毛边照旧（HANDOFF-2026-09-11 §六）

## 五、工作纪律

同前：每个小步落 commit + 部署即验证——断线零损失，`kimi --continue` 接着来。
PromptEval 用法：`dotnet run --project tools/PromptEval -- --scenario all --k 2`（报告落 logs/prompt-eval/，含原始输出供盲评）。
