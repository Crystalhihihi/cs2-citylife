# 交接文档 · 2026-09-14 终版（②普查全收口 + 剧场双修 + #65 监听定案）

**今日主线**：§12 落 #62-#65 → 开工①场所名小修 → ②普查 spike 全收口（侦察/工具/三轮实机）→ 剧场双修（末句被吃/自称自己）→ #65 监听口定案=集合差集法。
**commit 链**：`1af70cc`（§12 #62-#65）→ `4f3a62a`（①小修）→ `04ba778`（handoff）→ `50156e7`（普查工具 Ctrl+6/Ctrl+7）→ `93d79f4` → `ac49413`（普查首轮回填）→ `f11663d`（品牌真名层）→ `1ccc350` → `00f52c7`（剧场双修）→ `b16fc37` → `abea8f0`（探针改集合差集）→ `ee59c43`（⑤⑥ 收口回填）。
构建全绿（0 error），dll 部署至 22:3x。**下次开工前玩家需重启游戏才吃到最新 dll。**

## 一、未决现场（下次开工第一件事：实机复验清单）

进存档 `tail -f Logs/CityLife.log`，逐条过：
- **场所名**：气泡/剧场/环境摘要不再有"北美低密度商业"式 zone 标签；处境卡带品牌真名「」（"在「Cheap & Random」里逛街"）；信息流锚点/广告帖店名=品牌真名；活动选点分区建筑显示"方位·门牌号"
- **剧场双修**（`00f52c7`）：①窗口剧场能看到"路人一句、屋里人接一句"完整来回（此前末句 100% 被 Finished 时序 bug 吃掉）②无"大卫：大卫"式自称（prompt 样子勘误+FillSlots 自称守卫双保险；PromptEval 复测零自称 0 越界）
- **③对话场景卡**（`93323f4`，#63 已落地）：行人头顶应出现"名字A：…／名字B：…"两行对话泡（一条占 1 个同屏名额，凑不成对的仍是独白；车辆照旧独白）。日志看 `[闲聊炉]` 开炉"配对 N 对"与收炉对话计数；实机关注：①夜晚住宅区街上行人少，对话占比可能低于 6:4（尝试制设计如此）②长名字（>8字）市民拼装撞 40 字硬顶会被丢——看收炉解析丢计数
- **⚠ 快轨合规修复**（`7ed48f7`+`8c211e6`，2026-09-15 玩家报"街上对话和之前一样"定位）：**对卡 schema 此前从未在快轨模型上验过**（PromptEval 一直跑慢轨 v4-flash），生产快轨 V3 无 thinking 跟随率 50%/150% → 9 对配对只入库 2 条对话。已修：prompt 输出样子+红线+双人卡号点名（V3 schema 跟随 100%）、句长闸 12→14 收编擦边（复测双具齐全率 100%/100%）、收炉"对话缺口"诊断日志。**实机已验**（12:55 会话）：配对→入库 11/12 ≈92%（修复前 22%）。**教训落档：离线评测必须覆盖生产实际用的那条轨**
- **⚠ 剧场节拍回归修复**（`cd54a38`，同场日志顺带逮到）：#68 把评估拍/地点冷却也挂上了选项时钟，默认墙钟档把 3-4-5 游戏分钟放大成 3-5 现实分钟（3x 下≈7 倍稀薄）→ 剧场 9 分钟零开播。已回归 #48：供给节奏（评估拍/冷却）恒锚游戏时间，#68 选项只管 HoldFor/换句显示时钟。**复验**：剧场开播频率应恢复到旧手感（3x 下≈每分钟都有戏）
- **#66-#68**（`54433c4`/`b8518a3`/`f674ebb`，2026-09-15）：①儿童进气泡——"小学生"身份词独白/被配对，口吻像不像小孩；信息流应仍无儿童发帖 ②节奏开关——设置页"气泡与剧场节奏随游戏倍速"（默认关=现实时间），开 3x 对比两档：街上气泡文字节奏应不随倍速（移动锚点离画快是物理真相，开关只管文字节奏）；剧场换句同理 ③街头短剧——路边 2-N 人轮播对话（剧场泡不占同屏名额），设置页"最大人数 2-5 默认 3/最长句数 2-12 默认 6"向下取；已知良性：候车人可能被抓进街头剧、街头冷却挂 agent 实体（表恒小）；关注开播率与 token 消耗
- **旧账**（HANDOFF-2026-09-11 §一）：猫密度、同屏上限 4（旧存档 .coc 存着 6，设置页手动拉）、传闻时效衰减毛边未动
- 玩家已反馈待修：**"超市永远排队/住宅永远装修暖气"→ 归 #62 业态细分治**（字段全通，未动工）

## 二、今日关键实锤（勿重探）

**场所名/普查**（报告 docs/spikes/2026-09-14-census-and-event-watch.md，七项未实锤全收口；产物 logs/census-spike-20260914/ + logs/event-watch-spike-20260914/）：
- **公司渲染名 34/34 全是 Assets. 脏键**——公司 prefab 无本地化名；**真店名在 `CompanyData.m_Brand` 品牌实体**（"Cheap & Random"式，与游戏 UI 同源）。`EnvironmentDigestSystem.CompanyNameOf` 共享口，ShopNameOf/CompanyLabel 收编（`f11663d`）
- 住户姓=真姓（"汉密尔顿一家"式，170/170）；prefab Localization 组件挂接面 0%（全走 Assets.NAME[prefab] 键查库）；公司产出 IndustrialProcessData 34/34 可读（含开采类）——**#62 业态细分字段全通**
- #62 斩断复查零误伤零漏网：zone-grown 渲染名 100% 是 zone 通用名，非自长全是真名
- 意外发现：实体侧 Building.m_Flags 全场同值存疑；开采公司挂在自长"农业区枢纽"当租户（养鱼场/温室零租户）；地址 n/a 恰全是 NoRoadConnection prefab

**事件监听口**（#65 地基，spike 报告 §3）：
- `Created`/`Deleted` 标记只活一帧；**GameSimulation 相位 watcher 看不到玩家放置的标记**（两轮零命中实锤相位错位：Apply 在 ApplyTool 相位、Cleanup 帧末清标记）——标记直读路线弃用
- **定案=集合差集法**（相位无关，Ctrl+7 探针已实证）：玩家手放（Playground02）与自长住宅（6 栋）均当帧检出、IsZoneGrown 判别全对
- **三条 #65 实操约束**：①修路预览噪音（86/86 新路段 0.4s-45s 内全配对消失）→ 反应层必须防抖"落地稳定 N 秒才算数" ②自长过滤（自长住宅在修路完工后 3 秒突发到达）→ 只收"自长=否"天然过滤 ③树 23.5 万/城 → 正式 watcher 不做全量树 diff
- **Owner 判别证伪**：玩家手种树 AppleTree 带 Owner、Spruce 不带——Owner 无法区分玩家种 vs 附带树；"玩家手种树"区分=开放问题（空间关系/Tool 侧信号，未验证）
- 可入刊事件 prefab 为空 → #64 灾难规模用 m_Event 回指针计数（HealthProblem=伤亡/Flooded·Destroyed·OnFire=受灾），别指望 EventJournal

**#65 性能定调**（玩家问"常开会不会卡"，答复口径）：不会常开全量 diff——降频 16/32 帧一拍（快轨 LLM 本身 5-15s，帧级无意义）+ 只 diff 建筑+路（~6000 实体）不做树全量 + 只读 Temp 分配不进热路径；设置页留总开关。探针（Ctrl+6/Ctrl+7）是 debug 工具默认关，"太卡"=忘关探针。

**剧场双修根因**（`00f52c7`，细节在代码注释）：末句被吃=OnAnchorRotated 推末句同拍置 Finished，TryGetLine 的 !Finished 闸拦在渲染外（窗口 2 句制必中招），Finished 语义改"末句被读完才置"；自称=prompt 占位样子把 speaker 1 台词写成 {name1}（模型镜像），样子勘误 {name2}+自称红线+FillSlots 自称守卫（speaker 自槽填下一位）。

**乌龙记录**：玩家报"气泡没了"=建造工具自动隐藏（§12 #46 正常行为），忘关工具所致，非 bug。

## 三、下一步 backlog（玩家二选一后按序）

1. ~~**③对话场景卡**~~ **已完成（`93323f4`，2026-09-15）**：成对采样=组炉时 Walk 卡 60% 槽位 CollectAround(12m) 找步行 B；schema `{"card","a","b"}` 各≤12字；执行层拼 `名字A：a\n名字B：b` 单条 Text（渲染零改动）；PromptEval 前后对比齐全率 6/6、格式零回归。实机复验见 §一
2. ~~**#66/#67/#68**~~ **已完成（`54433c4`/`b8518a3`/`f674ebb` + 设计档 `e8d7479`，2026-09-15）**：儿童进气泡（池带年龄段，主炉滤、闲聊/配对放行）；节奏倍速开关（默认现实时间，剧场计时源全审计改档，高速档锚点打分偏好慢/静止）；街头闲谈剧场化（street 场景进库存炉+每炉≥1 部配额，ScanStreet 抓步行路人，向下取小演短演，设置页双旋钮）。PromptEval：street 出现率 2/8 合规、零回归。实机复验见 §一
3. **#62 业态细分**（治"超市永远排队/住宅永远装修暖气"）：商业按公司销售资源分（餐饮/便利/服饰…）、工厂按产出分、办公按 immaterial 分（软件/金融/电信/传媒=游戏 IsOfficeResource 掩码同源）、住宅无业态；ShopOutput 映射表扩工业/开采（现全落兜底"店"）；普查产物 logs/census-spike-20260914/ 有 Resource 全量位标志表
4. **④玩家动作反应层**（#65）：监听=集合差集法（16/32 帧档、只建筑+路、防抖 N 秒、只收自长=否）→ 圈周边市民（四叉树半径查询现成）→ 快轨反应卡，复用突发快讯/传闻榜管线；政策类后续；设置页留总开关
5. 远期：离线活人感实验矩阵（PromptEval 升级：单/多 agent×有无剧本×上下文加料×周围识别方式，盲评）；人机味专项（语气词/断句/不完整句/情绪标点，离线 A/B）
6. 毛边照旧节：传闻时效衰减、HasInsideRoom 标定、剧场 {place} 类别词兜底略生硬、突发快讯定位方位制（HANDOFF-2026-09-11 §六 + 本文 §四上一版）

## 四、工作纪律

同前：决策先落设计文档（§12）再写码；每个小步落 commit + 部署即验证；构建 `dotnet build`，部署 `bash scripts/deploy.sh`（改 dll 后**玩家需重启游戏**）；PromptEval `dotnet run --project tools/PromptEval -- --scenario all --k 2`（报告落 logs/prompt-eval/，prompt 改动前后必对比）；重活/长日志分析派子 agent 保主上下文；spike 产物归档 logs/（gitignore），报告落 docs/spikes/。
