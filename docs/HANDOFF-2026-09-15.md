# 交接文档 · 2026-09-15（对话场景卡 + 儿童/街头剧场/倍速选项 + 快轨合规修复 + 剧场节拍回归修复）

**前情**：②普查全收口、剧场双修、#65 监听定案见 [HANDOFF-2026-09-14.md](HANDOFF-2026-09-14.md)（其 §一 复验清单大部分仍有效）。

**今日主线**：③对话场景卡（#63）落地 → #66 儿童进气泡 / #67 街头闲谈剧场化 / #68 节奏倍速选项 → 玩家报"街上对话和之前一样"→ 定位快轨 V3 schema 合规惨案（9 对→2 条）→ prompt 强化+闸门收编修复，实机验证 11/12 → 同场日志顺带逮到 #68 剧场节拍回归（7 倍稀薄、9 分钟零开播）→ 回归 #48 修复。

**commit 链**：`93323f4`（③对话卡）→ `54433c4`（#66 儿童）→ `b8518a3`（#68 倍速选项）→ `f674ebb`（#67 街头剧场）→ `e8d7479`（设计档 #66-#68）→ `7ed48f7`（快轨 prompt 强化+诊断日志）→ `8c211e6`（句长闸 12→14）→ `cd54a38`（剧场节拍回归 #48）→ `fd87e5d` 前各步交接回填。构建全绿，dll 部署至 13:11。

## 一、未测清单（玩家睡醒后第一件事：重启游戏，tail -f CityLife.log）

`cd54a38`（剧场节拍）和 `8c211e6`（句长闸）**部署后尚未实机验证**。逐条过：

1. **剧场开播频率恢复**：`[剧场]` 应恢复旧手感的开播节奏（3x 下大约每分钟都有戏），且应出现 **street 街头短剧**（路边 2-N 人轮播、各占头顶、不占普通泡名额）。设置页新旋钮：最大人数 2-5 默认 3、最长句数 2-12 默认 6（向下取：人不够小演、句不足短演；设高了浪费 token 自负）
2. **对话泡密度**：`[闲聊炉]` "配对 N 对"→入库"对话 N 条"应 ≈1:1；街上能看到"名字A：…／名字B：…"双行泡（一条占 1 同屏名额）。夜里/低密度街区配对 0 对是正常（12m 内凑不到第二个步行市民）
3. **儿童气泡**：身份词"小学生"，可独白可被配对；信息流应仍无儿童发帖
4. **倍速开关**（默认关=现实时间）：3x 下文字节奏不加速；开=回游戏时钟、暂停冻结。移动锚点离画快是物理真相，开关只管文字节奏
5. 旧账照旧：场所名/品牌真名、剧场双修（完整来回/无自称）、猫密度、同屏上限 4

**若对话占比还嫌低**：备选旋钮=配对尝试率（现 Walk 卡 60% 槽位）/配对半径（现 12m）/Walk 卡保底（现 ≥3）——都是 BubbleChatterSystem 小改，等手感再定。

## 二、今日关键实锤（勿重探）

- **离线评测必须覆盖生产实际用的那条轨**（今日最大教训，已落 #63 条目）：闲聊炉生产走快轨硅基流动 DeepSeek-V3（thinking 关），而 PromptEval 默认 llm.json 是慢轨 deepseek-v4-flash——对卡 {"card","a","b"} schema 在 v4-flash 100% 合规，在 V3 无 thinking 下 50% 漏产/150% 超产。**修法**（对治无 thinking 模型有效）：prompt 给独白/对话各一行输出样子（中性句防镜像照抄，#61 教训）+红线明示后果+任务行点名双人卡号（"双人卡=卡 5、卡 8"）+按卡号顺序 → V3 schema 跟随 100%。残差=句长擦边（13-15 字），闸放宽 12→14 收编（prompt 目标仍 12，拼装 40 硬顶不变）
- **快轨模拟法**：`dotnet run --project tools/PromptEval -- --scenario chatter --k 3 --model deepseek-chat --thinking disabled`（官方 V3 系近似硅基快轨；key 只走 llm.json 不进上下文）
- **#68 边界**（已落档）：供给节奏（评估拍/地点冷却/发炉节拍）恒锚游戏时间（#48：暂停零成本、倍速同速放大）；阅读舒适度时钟（HoldFor/剧场换句显示）才走倍速选项（默认现实时间）。误挂墙钟档=3x 下内容稀薄 7 倍
- **快轨 V3 独白句长纪律偏弱**（>20 字占比 9-15%，v4-flash 41-46%）——"长短混排"规格在快轨天然打折，模型特性非 bug
- 对话缺口诊断日志：收炉时配对>0 且入库<配对自动打印原因分类（schema 未跟随/越界超产/拼装超40/解析丢）
- 良性已知：街头场冷却挂行人 agent 实体（表恒小）；候车人可能被抓进街头剧（roster 同源未细分，观感怪再治）；长名字（>8 字）市民拼装撞 40 硬顶会丢（看缺口日志）

## 三、下一步 backlog（按序）

1. **#62 业态细分**（治"超市永远排队/住宅永远装修暖气"）：商业按公司销售资源分、工厂按产出分、办公按 immaterial 分（IsOfficeResource 掩码同源）、住宅无业态；ShopOutput 映射表扩工业/开采；普查产物 logs/census-spike-20260914/ 有 Resource 全量位标志表
2. **④玩家动作反应层**（#65）：监听=集合差集法定案（16/32 帧档、只建筑+路、防抖 N 秒、只收自长=否）→ 圈周边市民 → 快轨反应卡；设置页留总开关
3. 远期：离线活人感实验矩阵（PromptEval 升级盲评）；人机味专项（语气词/断句/不完整句，离线 A/B）
4. 毛边照旧节：传闻时效衰减、HasInsideRoom 标定、剧场 {place} 类别词兜底、突发快讯定位方位制、候车人进街头剧观感

## 四、工作纪律与同会话资产

同 09-14 §四（决策先落 §12 再写码；dotnet build 0 error；deploy 后玩家需重启；每小步 commit 中文 conventional；PromptEval 前后对比；重活派子 agent）。

**子 agent resume id**（本会话可续）：agent-5=coder（闲聊炉/#63/快轨 prompt 上下文）、agent-6=coder（#66-#68/剧场/设置页上下文）、agent-4=explore（气泡管线全图）。日志：`%USERPROFILE%/AppData/LocalLow/Colossal Order/Cities Skylines II/Logs/CityLife.log`（每次启动游戏会截断重写，分析前先对时间戳）。
